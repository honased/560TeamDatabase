O List<Show> GetSimilarShows(UserID, ShowID) [KC]
List<string> GetTopGenres(UserID) // TOP Filter [EDH]
X List<Show> GetMyFavoriteShows(UserID) // TOP Filter [MW]
X List<Show> GetMyMostWatchedShows(UserID) // TOP Filter [MW]
X List<Show> SearchShows(UserID, Title, Director, ReleaseYear, Genre, SearchLibrary) [EDH]
X bool LoginUser(username) // In SQl, return true if found, false otherwise [MW]
X bool RegisterUser(username) // In SQL, return false if found, true otherwise. Also, if not found, insert into UserTable. [MW]

X AddShowToLibrary(UserID, ShowID) // Use Merge In Sql [KC]
X RemoveShowFromLibrary(UserID, ShowID) [KC]
X AddWatchLog(UserID, ShowID) [MW]
X ReviewShow(UserID, SHowID, Review) // Use Merge In SQL [EDH]

NEW INTERFACE METHODS
X List<Genre> GetAllGenres() [MW]